package es.ucm.fdi.trabajo;

import java.util.ArrayList;
import java.util.Collections;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import es.ucm.fdi.trabajo.function.Function;

/**
 * 
 * @author Eduardo Amaya
 * @author Javier Galiana
 * @author Eloy Mósig
 *
 */

//TODO BROMA, SE PASA A FUNCTION TO GRAPHIC

public class Grafico {
	/*private int dimension;
	//private Politopo[] objeto;
	private Map<Vertice, Vertice> objeto;
	private List<Vertice> dominio;
	private double resolucion;
	/**
	 * Aproxima la funciï¿½n f mediante un confunto de politopos con la resoluciï¿½n
	 * deseada
	 * 
	 * @param f
	 * @param res
	 */
	/*public void Grafico(int dimension) {
		this.dimension = dimension; //n+1 si la función es de R^n a R
		objeto = new HashMap<>();
		//inicializo aquí objeto con dos vértices en el origen?
		double[] cmps = new double[dimension];
		for(int i = 0; i < dimension; ++i)
			cmps[i] = 0;
		objeto.put(new Vertice(dimension, cmps), new Vertice(dimension, cmps));
	}
	
	public void Generar(Function f, List<Vertice>dominio, int res) {
		this.dominio = dominio; //si esto es A, voy a dibujar f(A)
		resolucion = 1/2^res;
		f.evaluate(x);
		
	}*/
}
